yarn-error.log
package-lock.json
node_modules/**/*
.yarn
.expo/

# Keep yarn.lock tracked for reproducible EAS builds
# (overrides the monorepo-root .gitignore rule that ignores all yarn.lock)
!yarn.lock
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# eas update
dist/

# local env overrides
.env*.local

# CNG-generated native projects
ios/
android/
