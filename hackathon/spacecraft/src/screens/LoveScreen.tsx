import { View, Text } from "react-native";
import React from "react";

interface Props {}

export const LoveScreen = (props: Props) => {
  return (
    <View>
      <Text>LoveScreen</Text>
    </View>
  );
};
